spring:
  main:
    banner-mode: off
  test:
    database:
      replace: none
  datasource:
    url: jdbc:h2:mem:rbacDb;
    username: sa
    password:
    driver-class-name: org.h2.Driver
mybatis-plus:
  global-config:
    banner: false